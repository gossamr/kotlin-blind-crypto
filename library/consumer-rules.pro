# Consumer ProGuard rules for kotlin-blind-crypto.
# Intentionally empty: the library exposes a small public API and relies on
# its dependencies (curve25519-kotlin, cryptography-kotlin) to ship their own
# consumer rules where needed. Add rules here only if a downstream consumer
# reports a stripped reference into this library.
